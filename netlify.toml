
# Netlify 배포 설정 파일

[build]
  # 빌드 명령어 (package.json의 scripts 참고)
  command = "npm run build"
  # Vite가 빌드 결과물을 출력하는 기본 폴더
  publish = "dist"

# Single Page Application(SPA) 리다이렉트 설정
# React 라우팅을 사용할 때, 새로고침 시 404 에러가 나는 것을 방지합니다.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
